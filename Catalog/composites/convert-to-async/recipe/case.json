{
  "refactoring": "convert-to-async",
  "description": "Runs the recipe: Make Method Async on the method, which leaves its callers blocking on the task, then on each caller that can be async, up to a constructor, which keeps blocking",
  "steps": [
    {
      "refactoring": "make-method-async",
      "target": { "symbol": "M:Stock.Inventory.Available(System.String)" }
    },
    {
      "refactoring": "make-method-async",
      "target": { "symbol": "M:Stock.Inventory.InStock(System.String)" }
    },
    {
      "refactoring": "make-method-async",
      "target": { "symbol": "M:Stock.Report.Summary(Stock.Inventory)" }
    }
  ]
}
