{
  "refactoring": "convert-to-async",
  "description": "A void method that waits on a task returns a Task; a caller that is a statement awaits it, and a call in a lambda, which would change the delegate, blocks",
  "target": { "symbol": "M:Data.Repository.Save" }
}
