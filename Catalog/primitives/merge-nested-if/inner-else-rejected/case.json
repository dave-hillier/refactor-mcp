{
  "refactoring": "merge-nested-if",
  "description": "Refuses an inner if with an else, which would then run when the outer condition fails",
  "target": { "file": "Sample.cs", "caret": "marker" },
  "expect": "error",
  "errorCode": "inner-has-else"
}
