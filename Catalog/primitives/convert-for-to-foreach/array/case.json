{
  "refactoring": "convert-for-to-foreach",
  "description": "An index loop that only reads the array's elements becomes a foreach over the array",
  "target": {
    "file": "Sample.cs",
    "caret": "marker"
  }
}
